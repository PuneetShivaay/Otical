# Copy to .env.local and fill in real values. .env.local is gitignored.

# Resend — used by app/api/send/route.js for the contact form.
# Get a key at https://resend.com/api-keys (format: re_xxxxxxxx...)
RESEND_API_KEY=
RESEND_FROM_EMAIL=
RESEND_TO_EMAIL=
